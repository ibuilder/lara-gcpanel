@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">{{ __('Edit Attic Stock') }}</div>

                    <div class="card-body">
                        <form method="POST" action="{{ route('attic_stocks.update', $atticStock->id) }}">
                            @csrf
                            @method('PUT')

                            @include('modules.Closeout.attic_stocks._form')

                            <div class="row mb-0">
                                <div class="col-md-8 offset-md-4">
                                    <button type="submit" class="btn btn-primary">
                                        {{ __('Update') }}
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection